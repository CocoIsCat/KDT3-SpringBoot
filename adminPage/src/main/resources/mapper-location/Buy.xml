<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.adminpage.mapper.BuyMapper">

    <select id="cntBuy" resultType="int">
        select
            count(*)
        from
            buy
    </select>

    <select id="selectBuy" resultType="com.example.adminpage.dto.Buy">
        select
            *
        from
            buy
    </select>
</mapper>